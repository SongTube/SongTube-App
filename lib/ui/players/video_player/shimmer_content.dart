import 'package:flutter/material.dart';

class VideoPlayerShimmerContent extends StatelessWidget {
  const VideoPlayerShimmerContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
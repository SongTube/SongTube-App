import 'package:flutter/material.dart';

class VideoPlayerDescriptionPage extends StatelessWidget {
  const VideoPlayerDescriptionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}